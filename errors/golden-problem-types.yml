generated: '2026-08-14'
method: searched
source: >-
  openapi/golden-openapi-original.json, confirmed against the live spec at
  https://docs.golden.com/openapi.json and one live unauthenticated call
format: json
envelope:
  description: >-
    Errors are returned as application/json with a machine-readable `code`
    enum, a human-readable `detail` string, and a nullable `attr` naming the
    offending attribute (a drf-standardized-errors style envelope). 400
    validation errors return a list under `errors[]` with `code`/`detail`/`attr`.
  fields:
  - code
  - detail
  - attr
problems:
- status: 400
  code: validation_error
  title: Validation error
  detail: One or more request parameters failed validation.
  remediation: Check query parameters (ids, entityTypeIds, predicateIds, filterPredicateId, filterValue, cursor, pageSize) against the schema.
  source_operations:
  - openapi/golden-openapi-original.json#public_api_entities_list
  - openapi/golden-openapi-original.json#public_api_queries_results_list
- status: 403
  code: permission_denied
  title: Permission denied
  detail: The API key is missing, invalid, or not permitted to access the resource.
  remediation: Supply a valid `apikey` header with a plan that grants access.
  source_operations:
  - openapi/golden-openapi-original.json#public_api_entities_retrieve
- status: 404
  code: not_found
  title: Not found
  detail: The requested entity, query, entity type, or predicate does not exist.
  remediation: Verify the id/permalink; use the schema and list endpoints to discover valid identifiers.
  source_operations:
  - openapi/golden-openapi-original.json#public_api_entities_retrieve
  - openapi/golden-openapi-original.json#public_api_schema_predicates_retrieve
- status: 429
  code: throttled
  title: Throttled
  detail: The request was rate limited.
  remediation: Back off and retry after the throttle window; reduce request rate or upgrade the plan.
  source_operations:
  - openapi/golden-openapi-original.json#public_api_entities_list
- status: 500
  code: error
  type: server_error
  title: Server error
  detail: An unexpected server error occurred.
  remediation: Retry with backoff; contact Golden support if it persists.
  source_operations:
  - openapi/golden-openapi-original.json#public_api_entities_list
x-evidence:
  checked: '2026-08-14'
  probes:
  - url: https://golden.com/api/v2/public/entities/?pageSize=1
    status: 403
    content_type: application/json
    body: >-
      {"type":"client_error","errors":[{"code":"permission_denied","detail":"You
      didn't pass a valid API Key in the 'apikey' header.","attr":null}]}
    finding: >-
      The live API emits exactly the envelope the spec declares — a top-level
      `type` discriminator plus an `errors[]` array of {code, detail, attr}. The
      derived catalog above is confirmed against production, not only against
      the document.
  - url: https://docs.golden.com/openapi.json
    status: 200
    finding: >-
      429 `throttled` ("Request was throttled.") is declared on every operation;
      no application/problem+json media type is used anywhere, so this is a
      vendor envelope rather than RFC 9457 problem details.
